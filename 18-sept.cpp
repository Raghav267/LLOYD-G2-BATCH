#include <iostream>
using namespace std;
int main()
{

    // int a = 10;

    // int *ptr = &a;

    // int **nextPtr = &ptr;

    // cout << "The value present at the address of ptr is: " << *ptr << endl;
    // cout << "The value present at the address of nextPtr is: " << **nextPtr << endl;
    //     The address stored in ptr is: 0x61ff08
    // The address stored in nextPtr is: 0x61ff04

    // double a = 10.5;
    // double *ptr = &a;

    // cout << "The address where a is stored is: " << &a << endl;

    // cout << "The address stored in ptr is: " << *nextPtr << endl;

    // int a =100;
    // int b= 200;

    // int*aPtr = &a;
    // int*bPtr = &b;

    // int **nextPtr = &aPtr;

    // int c = **nextPtr + *bPtr;
    // cout << "The sum of a and b is: " << c << endl;

    int *ptr = NULL;
    cout << ptr << endl;
    cout << *ptr << endl;
    return 0;
}