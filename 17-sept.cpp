#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    //     cout << "Size of int: " << sizeof(int) << " bytes" << endl;
    //     cout << "Size of float: " << sizeof(float) << " bytes" << endl;
    //     cout << "Size of double: " << sizeof(double) << " bytes" << endl;
    // //     Size of int: 4 bytes
    // // Size of float: 4 bytes
    // // Size of double: 8 bytes

    // int a, b;
    // cout << "Enter two numbers: ";
    // cin >> a >> b;
    // cout << "The two numbers are: " << a << " and " << b << endl;

    // int i = 0;
    // for (; i < 10;)
    // {
    //     i = i + 2;
    //     cout << "Harsh Raghav" << endl;
    // }

    // int n;
    // cin >> n;

    // cout << "Sqauare root of the n is : " << sqrt(n) << endl;

    // int a, b, c;
    // cin >> a >> b >> c;

    // Check for Determetestic Value
    // float d = b * b - 4 * a * c;

    // if (d < 0)
    // {
    //     cout << -1 << endl;
    // }

    // else
    // {
    //     int root1 = (-b + sqrt(d)) / (2 * a);
    //     int root2 = (-b - sqrt(d)) / (2 * a);

    //     cout << root1 << " " << root2 << endl;
    // }

    // int a = 10;

    // cout << sizeof(a) << endl;

    // double b = a;
    // cout << b << endl;
    // cout << sizeof(b) << endl;

    // double value = 1100.55566;

    // int a = value;
    // cout<<

    int a = 19;
    int b = 2;

    float ans = (a * 1.0) / b;
    cout << ans << endl;

    return 0;
}